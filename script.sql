#Script para crear la tabla categorías

create table categorias(id int(40) PRIMARY KEY NOT NULL AUTO_INCREMENT, datetime VARCHAR(60) NOT NULL,
nombre varchar(120) NOT NULL, creador varchar(120) NOT NULL);
